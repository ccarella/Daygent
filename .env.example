# Supabase Configuration
# Get these from your Supabase project dashboard: https://app.supabase.com
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# CRITICAL: Required for organization creation and administrative operations
# This key bypasses Row Level Security - keep it secure!
# Without this, users will get 500 errors when creating organizations
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# GitHub App Configuration
# Create a GitHub App at: https://github.com/settings/apps/new
GITHUB_APP_ID=your_github_app_id
GITHUB_APP_NAME=your_github_app_name
GITHUB_APP_CLIENT_ID=your_github_app_client_id
GITHUB_APP_CLIENT_SECRET=your_github_app_client_secret
GITHUB_APP_WEBHOOK_SECRET=your_github_app_webhook_secret
# Private key should be base64 encoded: base64 -i private-key.pem
GITHUB_APP_PRIVATE_KEY=your_github_app_private_key_base64_encoded

# Optional: Override the app URL for GitHub callbacks (defaults to current origin)
# NEXT_PUBLIC_APP_URL=https://your-app.vercel.app

# For production deployment on Vercel:
# 1. Add all these variables to your Vercel project settings
# 2. Go to Settings → Environment Variables  
# 3. Add each variable for all environments (Production, Preview, Development)
# 4. Redeploy after adding variables
# See VERCEL_ENV_FIX.md for troubleshooting deployment issues