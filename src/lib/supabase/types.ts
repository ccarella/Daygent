// This file will contain generated types from Supabase
// Run `npm run db:types` to generate types (command to be added)
// For now, export empty to establish the file structure

export type Database = object;

// Re-export common Supabase types for convenience
export type { User, Session } from "@supabase/supabase-js";
